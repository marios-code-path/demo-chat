spring:
  cassandra:
    base-packages: com.demo.chat.persistence.cassandra.repository
    keyspace-name: chat_${app.key.type}
    local-datacenter: datacenter1
    # contact-points and port are set at launch (chat-build core --cassandra)
    # or by DynamicPropertySource in tests.
