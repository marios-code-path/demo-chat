spring:
  application:
    name: shell-test
app:
  client:
    discovery:
      config:
        pubsub:
          prefix: pubsub.
        index:
          prefix: index.
        persistence:
          prefix: persist.
        key:
          prefix: key.
        message:
          prefix: message.
        secrets:
          prefix: secrets.
        topic:
          prefix: topic.
        user:
          prefix: user.